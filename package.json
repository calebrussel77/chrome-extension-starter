{
  "name": "ai-translator-pro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter extension dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter extension build",
    "build:web": "pnpm --filter web build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter extension preview"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "typescript": "^5.5.3",
    "typescript-eslint": "^8.0.1"
  }
}
